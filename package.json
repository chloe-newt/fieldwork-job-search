{
  "name": "fieldwork-job-search",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone local opportunity intelligence and application tracking",
  "type": "module",
  "engines": { "node": ">=24.0.0 <25" },
  "scripts": {
    "start": "node server.mjs",
    "search": "node server.mjs --search-once",
    "test": "node --test tests/*.test.mjs",
    "backup": "node server.mjs --backup"
  },
  "dependencies": {}
}
